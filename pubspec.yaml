name: password_manager
description: A premium password manager app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_slidable: ^3.0.1
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  share_plus: ^7.2.1
  path_provider: ^2.1.1

flutter:
  uses-material-design: true